# Next.js
node_modules
.next
out
tsconfig.tsbuildinfo
.env
.env.local

# Capacitor / Android — build output and machine-specific files, never committed.
# The Android *project* (android/) itself IS committed — see MOBILE_BUILD.md.
android/local.properties
android/.gradle
android/app/build
android/build
android/captures
android/*.iml
android/.idea
*.apk
*.aab

# Capacitor's copied web assets are regenerated by `npx cap sync` — no need to commit them.
android/app/src/main/assets/public
